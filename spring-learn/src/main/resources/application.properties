# Application configuration
# Example: change the default server port
# server.port=8081

# Example: set application name (shows up in logs)
spring.application.name=spring-learn

# Turn on DEBUG logging for our own package so we can see the
# constructor / getter / setter / main() log statements
logging.level.com.cognizant=DEBUG
